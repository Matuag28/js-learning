// Immediately Invoked Function Expressions (IIFE)

(function chai () {
    console.log(`connected`);
    
})();

// we use this because we dont want to pollute our code from global scope 
// explicitly  include this semicolon ;
